info:
  name: DELETE /api/authentication/:EndUserID
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseURL}}/api/authentication/{{CallingEndUserID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
