info:
  name: DELETE /api/log/:LogID
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/api/log/{{LogID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
