info:
  name: GET /api/enduser/:EndUserID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/enduser/{{EndUserID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
