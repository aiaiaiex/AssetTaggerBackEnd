info:
  name: GET /api
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api"
  auth: inherit

runtime:
  assertions:
    - expression: res.body.databaseReachable
      operator: eq
      value: "true"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
