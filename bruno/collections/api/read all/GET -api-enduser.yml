info:
  name: GET /api/enduser
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/enduser"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
