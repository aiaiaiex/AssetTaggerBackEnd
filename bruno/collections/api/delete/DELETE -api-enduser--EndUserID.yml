info:
  name: DELETE /api/enduser/:EndUserID
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/api/enduser/{{EndUserID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
