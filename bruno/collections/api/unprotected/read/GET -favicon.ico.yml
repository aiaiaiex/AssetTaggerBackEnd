info:
  name: GET /favicon.ico
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/favicon.ico"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
